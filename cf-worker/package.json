{
  "name": "stage-previz-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker — Google Drive CORS proxy for stage-previz",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
